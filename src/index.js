import PageAlbuns from "./pages/Albuns";

import "./assets/css/global.css";

PageAlbuns();

/*
const pessoa = {
  nome: "Jaison",
  idade: 36,
  endereco: "Passo Fundo",
  pais: "Brasil",
  nacionalidade: "brasileiro",
};

function recebePessoa({ nome = "", idade = "", ...props }) {
  console.log(nome + " " + idade);

  console.log(`${nome == "" ? "fulano" : nome} ${idade}`);
}

recebePessoa(pessoa);


const [variavel, setVariavel] = setState() spread


*/
